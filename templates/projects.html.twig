<section class="d-flex flex-direction-column">
    <div class="absolute left-50">
        <span class="vertical-span-black"></span>
        <p class="fs-18">Projects</p>
    </div>
{%   for project in projects   %}
    <div class="h-100vh"></div>
        {{   project.porTitle   }}
        {{   project.porShortDesc   }}
    {%   endfor   %}
</section>